# Deploy-HOST dependencies for running adk/deploy_agent_engine.py.
#
# These are NOT part of the deployed agent runtime (that's requirements.txt) —
# they are only needed on the machine that performs the Agent Engine deploy.
#
#     pip install -r adk/requirements-deploy.txt
#
# google-cloud-aiplatform provides `vertexai`, `vertexai.agent_engines`, and
# `vertexai.preview.reasoning_engines.AdkApp`; the extras pull the Agent Engine
# packaging + ADK support. google-cloud-storage powers the staging-bucket
# preflight check.
google-cloud-aiplatform[agent_engines,adk]>=1.95
google-cloud-storage>=2.16
