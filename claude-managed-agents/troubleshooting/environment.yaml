name: endor-troubleshooting
config:
  type: cloud
  networking:
    type: limited
    allowed_hosts:
    - api.endorlabs.com
    allow_mcp_servers: false
    allow_package_managers: true
  packages:
    npm:
    - endorctl
